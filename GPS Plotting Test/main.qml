import QtQuick
import QtQuick.Controls
import QtPositioning
import QtLocation
import GPS_Plotting_Test // Updated to match the new URI

Window {
    width: 360
    height: 740
    visible: true
    title: qsTr("GPS Plotting Test")

// ... the rest of your QML code remains exactly the same ...